============
Installation
============

At the command line::

    $ easy_install django-coap

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-coap
    $ pip install django-coap
